Interview: The Linux file system is a hierarchical structure starting from the root directory ‘/’, where all files, directories, and devices are organised in a unified tree-like format.

The Linux file system is the way Linux organizes and stores files and directories.

Everything starts from a single root directory: /
Unlike Windows (C:, D:), Linux has a single unified directory tree
